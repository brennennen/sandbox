
#ifndef REGISTERS_RV64I_H
#define REGISTERS_RV64I_H

#include <stdint.h>

typedef struct {

    // TODO...
    // ...

    // On RISCV pc is not a general purpose register, todo: figure out size?
    uint32_t pc;
} registers_rv64i_t;



#endif // REGISTERS_RV64I_H
