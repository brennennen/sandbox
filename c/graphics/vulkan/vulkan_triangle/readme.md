
## Setup
* Requires Vulkan (interfaced through with volk) - https://vulkan.lunarg.com/sdk/home
* Add Vulkan binaries dir to path (for compiling shaders, need tool: glslc)
* Run `./setup.py`
* Uses zig to build - https://ziglang.org/download/

## Build
`zig build`

## Run
`zig build run`
